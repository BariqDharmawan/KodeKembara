import CareerSkillRequirementFactory from 'App/Models/CareerSkillRequirement'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(CareerSkillRequirementFactory, ({ faker }) => {
  return {
    //
  }
}).build()
